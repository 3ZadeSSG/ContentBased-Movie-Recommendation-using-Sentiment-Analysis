<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/PrimaryPurple</item>
        <item name="colorPrimaryDark">@color/PurpleMiddle</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppThemeNavigation" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/PrimaryPurple</item>
        <item name="colorPrimaryDark">@color/PurpleMiddle</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="textBoxStyle">
        <item name="android:elevation">8dp</item>
        <item name="android:textColor">@color/Black</item>
        <item name="android:background">@drawable/my_textbox_layout</item>
        <item name="android:shadowColor">@color/Black</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="buttonStyle">
        <item name="android:elevation">8dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:background">@drawable/button_background</item>
        <item name="android:shadowColor">@color/Black</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/TextWhite</item>
        <item name="android:textAllCaps">false</item>
    </style>
</resources>
